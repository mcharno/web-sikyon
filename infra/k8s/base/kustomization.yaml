apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: burnside

resources:
  - namespace.yaml
  - ghcr-sealed-secret.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml
  - ingress-external.yaml

commonLabels:
  app.kubernetes.io/name: sikyon
  app.kubernetes.io/managed-by: argocd

# Image tags are managed directly in deployment YAMLs by GHA build workflows.
# Do not add an images: override block here — it would override the SHA tags.
